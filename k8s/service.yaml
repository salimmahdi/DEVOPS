apiVersion: v1
kind: Service
metadata:
  name: devops-app-service
  labels:
    app: devops-app
spec:
  type: NodePort
  selector:
    app: devops-app
  ports:
  - protocol: TCP
    port: 8089
    targetPort: 8089
    nodePort: 30089